
  # Pete playground

  This is a code bundle for Pete playground. The original project is available at https://www.figma.com/design/76jgepHfivsEz7psyn1pFU/Pete-playground.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  